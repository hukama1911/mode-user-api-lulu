name: client_server_vita
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"  # Batasan SDK yang tepat untuk Flutter

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_styled_toast: ^2.0.0
  cached_network_image: ^3.4.1
  intl: ^0.19.0
  google_maps_flutter: ^2.12.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  http: ^1.4.0

flutter:
  uses-material-design: true
  assets:




